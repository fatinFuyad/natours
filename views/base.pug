doctype html 
html(lang="en")
  head
    block head
      meta(charset="UTF-8")
      meta(name="viewport" content="width=device-width, initial-scale=1.0") 
      //- meta(http-equiv="Content-Security-Policy" content="style-src 'self'; font-src 'self' 'script-src-elem' https://fonts.googleapis.com/css?family=Lato:300,300i,700; script-src 'self' 'script-src-elem' https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js;") 
      //- meta( http-equiv="Content-Security-Policy" content="connect-src 'self' https://cdn.jsdelivr.net")
      link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato:300,300i,700")
      link(rel="stylesheet" href="/css/style.css")
      link(rel="shortcut icon" type="image/png" href="/img/favicon.png")
      script(src="/js/bundle.js" defer )
      title Natours | #{title}
  body
    // HEADER
    include _header

    // MAIN CONTENT
    block content
      h1 This is a placeholder heading  

    // FOOTER 
    include _footer  

    //- script(src="/js/bundle.js" )
    //- script(src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js")
    //- script(src='/js/login.js' defer)
    //- script(src="/js/mapbox.js" defer )